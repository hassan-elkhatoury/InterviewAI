<?xml version="1.0" encoding="UTF-8"?>
<?import javafx.geometry.Insets?>
<?import javafx.scene.layout.*?>
<?import javafx.scene.control.*?>
<?import javafx.scene.image.*?>
<StackPane xmlns:fx="http://javafx.com/fxml/1" fx:controller="com.interviewai.controller.CreateAccountController"
           stylesheets="@../css/main.css" styleClass="dark-root">
    <BorderPane>
        <top>
            <HBox alignment="CENTER_RIGHT">
                <padding>
                    <Insets top="16" right="24" bottom="0" left="24" />
                </padding>
                <Pane HBox.hgrow="ALWAYS" />
                <Button text="Se connecter" onAction="#onBack" styleClass="btn-outline-light" />
            </HBox>
        </top>
        <center>
            <VBox alignment="CENTER" spacing="18">
                <VBox alignment="CENTER" spacing="18" styleClass="auth-dark" maxWidth="520">
                    <Label text="Créer un compte" styleClass="title-dark" />

                    <!-- Username -->
                    <VBox spacing="8">
                        <TextField fx:id="usernameField" promptText="Nom d'utilisateur" styleClass="input-dark" />
                    </VBox>

                    <!-- Email -->
                    <VBox spacing="8">
                        <TextField fx:id="emailField" promptText="E-mail" styleClass="input-dark" />
                    </VBox>

                    <!-- Password -->
                    <VBox spacing="8">
                        <PasswordField fx:id="passwordField" promptText="Mot de passe" styleClass="input-dark" />
                    </VBox>

                    <!-- Primary button -->
                    <Button fx:id="createAccountButton" text="S'INSCRIRE" onAction="#onCreate" defaultButton="true" styleClass="btn-primary-blue" maxWidth="Infinity" />

                    <!-- Divider -->
                    <HBox alignment="CENTER" spacing="10">
                        <Region HBox.hgrow="ALWAYS" prefHeight="1" style="-fx-background-color:#233041;" />
                        <Label text="OU" styleClass="divider-label" />
                        <Region HBox.hgrow="ALWAYS" prefHeight="1" style="-fx-background-color:#233041;" />
                    </HBox>

                    <!-- Social buttons -->
                    <HBox spacing="16" alignment="CENTER">
                        <Button text="GOOGLE" styleClass="btn-social" />
                        <Button text="FACEBOOK" styleClass="btn-social" />
                    </HBox>

                    <!-- Footer text -->
                    <Label text="En t'inscrivant à InterviewAI, tu acceptes nos Conditions d'utilisation et notre Politique de confidentialité." wrapText="true" styleClass="footer-text" />
                </VBox>
            </VBox>
        </center>
    </BorderPane>
</StackPane>
